- name: system | create ~/.* directories
  file:
    path: "{{ ansible_env.HOME }}/.{{ item }}"
    mode: "744"
    state: directory
    recurse: yes
  with_items:
    - bash_configs.d

- name: system | install $HOME/.*
  copy:
    src: "{{ item }}"
    dest: "{{ ansible_env.HOME }}/.{{ item }}"
    mode: "644"
  with_items:
    - bash_configs.d/bash.sh
    - bash_configs.d/git.sh
    - bash_configs.d/go.sh
    - bash_configs.d/kubernetes.sh
    - bash_configs.d/python.sh
    - bash_configs.d/yandex.sh
    - bash_aliases
    - bash_profile
    - bashrc
    - inputrc
    - tmux.conf

- import_tasks: debian.yml
  when: ansible_os_family == "Debian"
