- name: vim [centos] | update repositories cache [yum]
  yum: update_cache=yes
  changed_when: False

- name: vim [centos] | upgrade out of date packages, excluding kernel [yum]
  yum:
    name: '*'
    state: latest
    exclude: kernel*

- name: vim [centos] | install the 'Development tools' package group
  yum:
    name: "@Development tools"
    state: present

- name: vim [centos] | install requirements [vim]
  package: name="{{ item }}"
  with_items: "{{ vim_requirements_centos }}"
