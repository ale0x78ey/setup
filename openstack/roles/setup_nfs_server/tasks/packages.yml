- name: setup_nfs_server | update repositories cache
  apt: update_cache=yes
  changed_when: False
  when: ansible_os_family == "Debian"

- name: setup_nfs_server | install requirements
  package: name="{{ item }}"
  with_items: "{{ ansible_requirements + nfs_server_requirements }}"

- name: setup_nfs_server | upgrade any out of date packages
  apt: upgrade=safe
  when: ansible_os_family == "Debian"
