vms:
  - name: kolla-build
    hostname: deploy
    users:
      - name: "{{ ansible_env.USER }}"
        public_key: "{{ ansible_env.HOME }}/.ssh/id_rsa.pub"
        groups:
          - sudo

  - name: kolla-ansible
    hostname: build
    users:
      - name: "{{ ansible_env.USER }}"
        public_key: "{{ ansible_env.HOME }}/.ssh/id_rsa.pub"
        groups:
          - sudo
