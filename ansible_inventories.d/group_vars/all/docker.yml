docker_insecure_registries:
  - "192.168.0.75:4000"

docker_requirements_debian:
  - docker-ce
  - docker-ce-cli
  - containerd.io

docker_requirements_pip:
  - docker

docker_configs:
  debian:
    repo: "deb https://download.docker.com/linux/ubuntu focal stable"
    gpg: https://download.docker.com/linux/ubuntu/gpg
