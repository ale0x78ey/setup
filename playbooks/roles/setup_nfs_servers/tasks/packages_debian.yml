- name: setup_nfs_servers [debian] | update repositories cache
  apt: update_cache=yes
  changed_when: False

- name: setup_nfs_servers [debian] | upgrade out of date packages
  apt: upgrade=safe

- name: setup_nfs_servers [debian] | install ansible requirements
  package: name="{{ item }}"
  with_items: "{{ ansible_requirements_debian }}"

- name: setup_nfs_servers [debian] | install nfs requirements
  package: name="{{ item }}"
  with_items: "{{ nfs_server_requirements_debian }}"
