openvpn_servers:
  routers:
    - proto: udp
      port: 1194
      mask: "255.255.255.0"
      cidr: "10.9.0.0/24"
      network: "10.9.0.0"
      dev: "{{ network_interface }}"

  bridges:
    - proto: udp
      port: 2194
      mask: "255.255.255.0"
      cidr: "10.9.0.0/24"
      network: "10.9.0.0"
      dev: "{{ neutron_external_interface }}"

openvpn_clients:
  routers:
    - hostname: "dell"
      vpn_ipv4: "10.9.0.2"
      vpn_mask: "255.255.255.0"
      dev: "{{ network_interface }}"

  bridges:
    - hostname: "dell"
      dev: "{{ neutron_external_interface }}"
